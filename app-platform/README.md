# App Platform

This repo is intended to hold the source-of-truth for App Platform configuration.

Current reality:
- StoryForge app spec/env is managed via DigitalOcean API updates.
- The application code lives in https://github.com/I0Q/storyforge

TODO:
- Export the current App Platform spec (app.yaml-like) here and treat it as source-of-truth.
